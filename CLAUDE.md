# CLAUDE.md

คำแนะนำสำหรับ Claude Code เมื่อทำงานกับโปรเจกต์นี้

## เกี่ยวกับโปรเจกต์

**ชื่อเว็บไซต์:** "ชีวิตดีๆ แม่วัยทองกับลูกวัยเรียน" (ห้ามเปลี่ยนชื่อนี้)

**กลุ่มเป้าหมาย:** คุณแม่วัย 40+ ที่มีลูกวัยเรียน

**บุคลิกของเว็บไซต์:**
- อบอุ่น
- เป็นมิตร
- น่าเชื่อถือ
- ดูทันสมัย
- อ่านง่าย
- ไม่ดูเป็นเว็บไซต์ที่สร้างโดย AI แบบสำเร็จรูป (หลีกเลี่ยง layout/สไตล์ที่ดู generic หรือ template จ๋าเกินไป)

**หลักการออกแบบ:**
- Mobile First — ออกแบบ/ทดสอบมุมมองมือถือก่อนเสมอ
- ใช้พื้นที่อย่างพอดี ไม่แน่นหรือโล่งเกินไป
- ตัวหนังสืออ่านง่าย ขนาด/บรรทัดเหมาะกับสายตาผู้ใหญ่
- ปุ่มสำคัญ (CTA) ต้องเห็นชัด
- ไม่ใช้สีมากเกินไป ยึดโทนสีเดิมของเว็บไซต์
- ให้ความรู้สึกอบอุ่นและเป็นธรรมชาติ

## โครงสร้างโปรเจกต์

โปรเจกต์นี้เป็นเว็บไซต์ static แบบไฟล์เดียว ไม่มี build tool / package manager:

- `index.html` — ไฟล์เดียวของทั้งเว็บไซต์ ประกอบด้วย HTML + CSS (ใน `<style>`) + JavaScript (ใน `<script>`) ทั้งหมดฝังอยู่ในไฟล์นี้
- ฟอนต์: โหลดจาก Google Fonts — `Chonburi` (หัวข้อ h1-h3) และ `Mitr` (เนื้อหาทั่วไป)
- สีหลักกำหนดผ่าน CSS variables ใน `:root` (เช่น `--bg`, `--ink`, `--rose`, `--sage`, `--butter`) — ควรใช้ variable เดิม ไม่ hardcode สีใหม่
- Section หลักของหน้า (ไล่ตามลำดับ): Header/Nav → Hero → About (`#about`) → Stories (`#stories`) → Activities (`#activities`) → Articles (`#articles`) → Facebook CTA → Footer
- JavaScript ท้ายไฟล์จัดการ 2 อย่าง: เมนูมือถือ (toggle) และ scroll-reveal animation ด้วย `IntersectionObserver` (มี fallback สำหรับ `prefers-reduced-motion`)
- ไฟล์มีขนาดใหญ่ (~1.3MB) เพราะมีรูปภาพฝังแบบ base64 อยู่ในไฟล์ — ระวังเวลาอ่าน/แก้ไฟล์ทั้งไฟล์ ควรอ่านเฉพาะช่วงบรรทัดที่เกี่ยวข้อง

## กฎการทำงาน

1. **ห้ามลบเนื้อหาเดิมโดยไม่ได้รับอนุญาต** — ถ้าจำเป็นต้องลบ/แทนที่เนื้อหา ให้ถามยืนยันก่อน
2. **ห้ามเปลี่ยนชื่อเว็บไซต์** "ชีวิตดีๆ แม่วัยทองกับลูกวัยเรียน" ไม่ว่ากรณีใด
3. **ห้ามติดตั้ง Library ใหม่ถ้าไม่จำเป็น** — โปรเจกต์นี้ตั้งใจให้เป็น static HTML ไฟล์เดียว ไม่มี framework/build step
4. **ก่อนแก้ไขไฟล์สำคัญ ให้ตรวจสอบโครงสร้างโปรเจกต์ก่อนเสมอ** (อ่านส่วนที่เกี่ยวข้องของ `index.html` ก่อนแก้)
5. **หลังแก้ไขให้ตรวจสอบว่าระบบยังทำงานได้** — เปิดไฟล์ในเบราว์เซอร์ ตรวจสอบว่า layout/เมนูมือถือ/scroll animation ยังทำงานปกติ ไม่มี syntax error ใน CSS/JS
6. **ถ้าพบ Error ให้บอกสาเหตุก่อนแก้ไข** — อธิบายให้เข้าใจก่อนลงมือแก้เสมอ
7. **อย่าเปลี่ยนส่วนอื่นของเว็บไซต์ถ้าไม่ได้อยู่ในคำขอ** — แก้เฉพาะจุดที่ถูกร้องขอ ไม่ refactor หรือปรับส่วนอื่นเพิ่มเอง
8. **ถ้า Requirement ไม่ชัดเจน ให้ถามก่อนทำ** อย่าเดาแล้วลงมือทำเอง
